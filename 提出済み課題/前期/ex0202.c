/*
24EO0118 原田健司
*/
#include <stdio.h>

int main(void)
{
	//変数の宣言
	double vx = 3.5;	//vxを宣言して3.5で初期化
	double vy = 2.4;		//vyを宣言して2.4で初期化

	//vxに代入した値を出力
	printf("vx : %f\n", vx);

	//vyに代入した値を出力
	printf("vy : %f\n", vy);

	//vx + vy の演算結果を出力
	printf("vx + vy = %f\n", vx + vy);

	return 0;
}
