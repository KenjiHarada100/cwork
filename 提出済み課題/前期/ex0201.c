/*
24EO0118 原田健司
*/
#include <stdio.h>

int main(void)
{
	//整数の変数の宣言
	int vx = 12;	//vxを宣言して12で初期化
	int vy = 6;		//vyを宣言して12で初期化

	//vxに代入した値を出力
	printf("vx : %d\n", vx);

	//vyに代入した値を出力
	printf("vy : %d\n", vy);

	//vx + vy の演算結果を出力
	printf("vx + vy = %d\n", vx + vy);

	return 0;
}
